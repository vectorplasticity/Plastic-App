# Plastic App

## View your App files on a sweet node graph.

### Installation

Install the repo to your Python environment:
```bash
pip install git+https://github.com/vectorplasticity/Plastic-App.git
```

then, run the app:
```bash
plastic-app
```

open browser to `127.0.0.1:5000` to veiw the app UI

